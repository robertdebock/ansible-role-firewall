---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
lint:
  name: yamllint
driver:
  name: vagrant
  provider:
    name: virtualbox
platforms:
  - name: firewall-debian
    box: debian/stretch64
  - name: firewall-fedora
    box: fedora/29-cloud-base
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: vagrant
