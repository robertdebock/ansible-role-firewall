---
# vars file for firewall
firewall_packages:
  required:
    - iptables
    - iptables-services
  conflicting:
    - firewalld
    - ufw

firewall_iptables_rulefile: /etc/sysconfig/iptables

firewall_service: iptables
